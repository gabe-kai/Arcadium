Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
python-dotenv==1.0.0
PyYAML==6.0.1
pytest==7.4.3
pytest-cov==4.1.0
# psycopg2-binary - PostgreSQL adapter
# Note: May have Python 3.14 compatibility issues. Install manually if needed:
#   pip install psycopg2-binary
# Or use psycopg2 (requires PostgreSQL dev libraries)
# Tests use SQLite in-memory for faster execution

