# Arcadium Project - Shared Python Dependencies
# This file contains dependencies for all Python services in the monorepo

# Wiki Service
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-CORS==4.0.0
# PostgreSQL adapter
# Note: psycopg2-binary 2.9.11+ supports Python 3.14
# If installation fails (build errors), use wheels only:
#   pip install psycopg2-binary --only-binary :all:
# Or use psycopg (version 3, better Python 3.14 support):
#   pip install "psycopg[binary]>=3.1.0"
psycopg2-binary>=2.9.11
python-dotenv==1.0.0
pytest==7.4.3
pytest-cov==4.1.0
watchdog==3.0.0
requests==2.31.0

# Add other service dependencies here as services are added

