# Shared Code

Shared code and libraries used across all services.

## Structure

- `protocols/` - API schemas, message formats, data models
- `auth/` - Authentication/authorization shared logic
- `utils/` - Common utilities and helpers

## Usage

Services can import shared code by mounting the shared directory or copying it into their build process.

